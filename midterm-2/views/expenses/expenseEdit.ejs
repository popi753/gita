<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/index.css">
    <title>Edit expense</title>
</head>
<body>
     <%- include("../partials/headers.ejs")  %>
    <main class="main">
            <div class="expense-card">
                <% if (expense) { %>
                        <h2><%= expense.category %></h2>
                        <h4><%= expense.price %>$</h4>
                        <% } else { %>
                            <h3>couldnt find the expense</h3>
                            <%}  %>                         
            </div>
            <h2>edit expense</h2>
            <form class="create-form" method="post" action="#">
                <label for="category">category</label>
                <input type="text" name="category"   placeholder="Enter category" value="<%= expense.category %>" required>

                <label for="price">price</label>
                <input type="number" name="price"  placeholder="Enter price"  value="<%= expense.price %>" required >

                <button>submit</button>
            </form>
            <div>
                <% if (error) { %>
                    
                    <% if (error.price) { %>
                    <h1><%= error.price %> </h1>
                    <% } %>
                    <% if (error.category) { %>
                        <h1><%= error.category %> </h1>
                    <% } %>
                    <% if (error.body) { %>
                        <h1><%= error.body %> </h1>
                    <% } %>
                    
                <% } %>
                
            </div>
     </main>
     <%- include("../partials/footer") %>

</body>
</html>